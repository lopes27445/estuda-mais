{
  "name": "estuda-mais-painel-lab",
  "version": "1.0.0",
  "private": true,
  "description": "Estuda+ — painel lab. Este package.json existe só para testar as regras do Firestore; o app em public/ não usa build nem dependência de npm.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-estuda-mais \"node --test test/rules.test.mjs test/gabaritos.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "firebase-admin": "^12.7.0",
    "firebase-tools": "^13.29.1"
  }
}
